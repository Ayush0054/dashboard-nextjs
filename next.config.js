/** @type {import('next').NextConfig} */
const nextConfig = {}

// module.exports = nextConfigs
module.exports = {
    images: {
      domains: ['lh3.googleusercontent.com'],
    },
    nextConfig
  }